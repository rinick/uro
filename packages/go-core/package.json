{
  "name": "@uro/go-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@sabaki/go-board": "^1.0.17",
    "@uro/sgf-core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.0.14"
  }
}
